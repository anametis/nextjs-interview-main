# Copy this file to `.env.local` and adjust values as needed.

# Default SWAPI base URL per README
NEXT_PUBLIC_BASE_URL=https://swapi.info/api/

# Axios request timeout in milliseconds
NEXT_PUBLIC_API_TIMEOUT_MS=3000
